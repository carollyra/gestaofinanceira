// Lowercase, without accents and with collapsed whitespace: used to compare
// descriptions, headers and category names regardless of formatting
export function normalizeText(value: string): string {
  return value
    .normalize('NFD')
    .replace(/\p{Diacritic}/gu, '')
    .toLowerCase()
    .replace(/\s+/g, ' ')
    .trim();
}
